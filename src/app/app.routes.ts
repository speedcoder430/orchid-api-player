import { Routes } from '@angular/router';
import { CameraViewerComponent } from './components/camera-viewer/camera-viewer.component';

export const routes: Routes = [
    { path: '', component: CameraViewerComponent }
];
